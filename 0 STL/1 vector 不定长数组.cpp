#include <bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    //定义T vector
    //vector<T> a;
    
    //预定大小len，每个T元素都会被构造
    //vector<T> a(int len);
    
    //准备len个T元素的内存空间，构造时不能先被预定大小
    //void a.reserve(int len);
    
    //初始化值
    //vector<T> a{1,2,3,4};
    
    //初始时复制另一个vector b的值过来
    //vector<T> a(b);
    
    //复制另一个vector b的值过来
    //a=b;
    
    //可以random access
    //a[id];
    
    //返回a的大小
    //int a.size();
    
    //返回a是否为空(长度为0)
    //bool a.empty();
    
    //改大小成len，多出来的会初始化为0
    //void a.resize(int len);
    
    //尾部加个元素x，size加1
    //void a.push_back(T x);
    
    //尾部加个以...为参数构造的元素，size加1，比push_back略快 
    //void a.emplace_back(...);
    
    //尾部删个元素x，size减1
    //void a.pop_back(T x);
    
    //查找首个元素
    //T a.front();
    
    //查找最后元素
    //T a.back();
    
    //清空
    //void a.clear();
    
    
    
    system("pause");
    return 0;
}
